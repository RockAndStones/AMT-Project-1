<p class="commentClass mb-4">
    ${comment.description}
    <span class="text-sm font-semibold">- ${comment.creator}, ${comment.date.dateFormatted()} at ${comment.date.timeFormatted()}</span>
</p>
